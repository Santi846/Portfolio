function palindrome(str) {

    const lowerCase = str.toLowerCase;
    
    const reverse = lowerCase.reverse;
    
    //const split = replace.split;
    
    const replace = reverse.replace;
    
    //const join = reverse.join;
    
    if (replace){
          return true;
    }
    else {
          return false;
    }
    
    }
    
    palindrome("eye");